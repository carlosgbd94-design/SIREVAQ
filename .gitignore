node_modules/
playwright-report/
test-results/
.env
.env*
!.env.example
*.env
*.key
*.json
!package.json
!deno.json
# Config del servidor de pruebas (playwright.config.js -> npx serve). Sin ella, en el CI serve usa
# cleanUrls y redirige /mobile.html a /mobile (la carpeta de la app React), rompiendo las pruebas de movil.
!serve.json
